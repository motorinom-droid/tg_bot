[project]
name = "tп_ище"
version = "0.1.0"
requires-python = ">=3.12"
readme = "README.md"
classifiers = [
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.12",
]
dependencies = [
    "aiogram>=3.26.0",
    "asyncio>=4.0.0",
]

[tool.uv]
package = false
